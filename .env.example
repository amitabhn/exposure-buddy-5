# EAS Build secrets — add real values via: eas secret:create --scope project --name <NAME> --value <value>
# NEVER commit real values to this file or any other committed file.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SENTRY_DSN=
